#include <iostream>
#include <filesystem>

void listFilesInDirectory(std::string path);
void fileExists(std::string path);
void readJson(std::string filePath);
void writeJson(std::string filePath, std::string data);